# SwarmClaw 开发日志 - 2026-03-03

## 安装与配置

### 1. 项目安装
```bash
git clone https://github.com/swarmclawai/swarmclaw.git /home/young/code/swarmclaw
cd /home/young/code/swarmclaw
npm install
npm run quickstart
```

### 2. 开机自启（生产模式）
创建 `~/.config/systemd/user/swarmclaw.service`：
```ini
[Unit]
Description=SwarmClaw AI Agent Orchestration
After=network.target

[Service]
Type=simple
WorkingDirectory=/home/young/code/swarmclaw
ExecStart=/usr/bin/npm run start -- -p 3456
Restart=always
RestartSec=10
Environment=PATH=/usr/local/bin:/usr/bin:/bin
Environment=NODE_ENV=production

[Install]
WantedBy=default.target
```

启用服务：
```bash
systemctl --user daemon-reload
systemctl --user enable swarmclaw.service
loginctl enable-linger young
```

先构建生产版本：
```bash
npm run build
systemctl --user start swarmclaw
```

### 3. 访问配置
- **局域网**: http://192.168.0.108:3456
- **Tailscale**: https://young-inspiron-3542.taild0ecaf.ts.net:3456
- **访问密钥**: 0634f30abfaaa0e6562a2ec886da5641

---

## 功能开发

### 1. 修复 crypto.randomUUID 兼容性问题

**问题**: 浏览器报错 `crypto.randomUUID is not a function`

**解决方案**:
1. 添加 polyfill 到 `src/app/layout.tsx`
2. 添加 webpack 配置到 `src/next.config.ts`

**提交**: `Fix crypto.randomUUID and add OpenClaw setup docs`

### 2. OpenClaw Agent ID 选择功能

**问题**: SwarmClaw 的 OpenClaw provider 硬编码使用 `main` agent，无法选择其他 agent

**解决方案**:
1. 在 `Agent` 类型添加 `openclawAgentId?: string` 字段
2. 修改 `src/lib/providers/openclaw.ts` 读取 agent 配置中的 `openclawAgentId`
3. 在 `src/components/agents/agent-sheet.tsx` 添加 UI 输入框

**影响文件**:
- `src/types/index.ts`
- `src/lib/providers/openclaw.ts`
- `src/components/agents/agent-sheet.tsx`

**提交**: `Add OpenClaw agent ID selection support`

### 3. 任务删除功能

**问题**: Task 界面没有删除按钮

**解决方案**:
1. 在 `src/components/tasks/task-sheet.tsx` 添加 "Delete" 按钮
2. 调用 `deleteTask` API

**提交**: `Add delete task button to task sheet`

### 4. 修复任务硬删除

**问题**: 删除任务时只是软删除（改为 archived 状态），定时任务仍引用旧任务 ID 继续创建

**解决方案**:
1. 修改 `src/app/api/tasks/[id]/route.ts` DELETE 方法
2. 从软删除改为硬删除（彻底移除）
3. 自动清除定时任务的 `linkedTaskId` 引用

**提交**: `Fix: Implement hard delete for tasks and clear schedule linkedTaskId`

---

## 集成配置

### OpenClaw Gateway 连接

1. **启动 OpenClaw Gateway**
```bash
systemctl --user start openclaw-gateway
```

2. **配置 Tailscale Serve**
```bash
# 修改 ~/.openclaw/openclaw.json
"tailscale": { "mode": "serve" }
```

3. **SwarmClaw Agent 配置**
- Provider: OpenClaw
- Gateway URL: `https://young-inspiron-3542.taild0ecaf.ts.net/v1`
- Gateway Token: `09e746e361686ad7cca06a9d28338f9aed8e6b33`
- OpenClaw Agent ID: `ai_daily`（或其他 agent）

---

## OpenClaw Agents 列表

| Agent ID | 名称 | 描述 |
|----------|------|------|
| `main` | Main Agent | 🦞 小明 |
| `product` | Product Agent | - |
| `skill` | Skill Manager | - |
| `ai_daily` | AI Daily Collector | 📰 AI Daily Collector |
| `ai_daily_architect` | Architect | 📰 AI Daily Collector |
| `ai_daily_researcher` | AI Researcher | 📰 AI Daily Collector |
| `ai_daily_coder` | Code Developer | 📰 AI Daily Collector |
| `ai_daily_tester` | QA Engineer | 📰 AI Daily Collector |
| `ai_daily_reviewer` | Code Reviewer | 📰 AI Daily Collector |
| `ai_daily_devops` | DevOps Engineer | 📰 AI Daily Collector |

---

## 功能总结

### Tasks vs Schedules

| 特性 | Task | Schedule |
|------|-------|-----------|
| **执行方式** | 手动或队列 | 自动定时 |
| **生命周期** | 一次性 | 持续运行 |
| **状态** | backlog → queued → running → completed/failed | active/disabled |
| **适用场景** | 临时任务、调试 | 定期报告、监控 |

### Memory 系统

**作用**: 长期记忆存储，允许 Agent 存储和检索持久化知识

**使用方式**:
1. Agent 配置中启用 `memory` tool
2. Agent 通过 `memory_tool` 操作记忆
3. Memory 页面管理所有记忆

**记忆范围**:
- Agent 记忆：只能被该 Agent 访问
- 全局记忆：所有 Agent 共享

---

## Git 提交记录

```bash
git checkout -b feature/openclaw-agent-select

# 提交 1: 修复 crypto.randomUUID
commit da817d3: Fix crypto.randomUUID and add OpenClaw setup docs

# 提交 2: 添加 OpenClaw agent ID 选择
commit 27c9264: Add OpenClaw agent ID selection support

# 提交 3: 添加删除任务按钮
commit 29f5378: Add delete task button to task sheet

# 提交 4: 修复硬删除
commit c39df89: Fix: Implement hard delete for tasks and clear schedule linkedTaskId
```

---

## 网络配置

### 代理配置
- **Mihomo (Clash)**: 127.0.0.1:7890
- **NO_PROXY**: localhost, 127.0.0.1, 100.91.104.58

### IP 地址
- **局域网**: 192.168.0.108
- **Tailscale**: 100.91.104.58
- **Tailscale 域名**: young-inspiron-3542.taild0ecaf.ts.net

---

## 服务状态

### SwarmClaw
```bash
systemctl --user status swarmclaw
# Active: active (running)
```

### OpenClaw Gateway
```bash
systemctl --user status openclaw-gateway
# Active: active (running)
```

---

## 端口映射

| 端口 | 服务 |
|------|------|
| 3456 | SwarmClaw HTTP |
| 3457 | SwarmClaw WebSocket |
| 18789 | OpenClaw Gateway (local) |
| - | OpenClaw Gateway (Tailscale Serve) |

---

## 技术栈

- **SwarmClaw**: Next.js 16 + React 19 + TypeScript
- **OpenClaw**: 本地 Gateway 服务
- **数据库**: SQLite (`data/swarmclaw.db`)
- **进程管理**: systemd user service
- **网络**: Tailscale VPN

---

## 待办事项

- [ ] 考虑在 Task 创建时添加定时选项（集成到 Schedule）
- [ ] 优化 OpenClaw agent 选择体验（下拉列表而不是手动输入）
- [ ] 添加 Memory 的使用示例到文档
